SimpleCode 分享版
================

1. 解压整个文件夹（不要只留一个 exe，同目录文件都要保留）。
2. 双击 SimpleCode.exe 启动。
3. 在设置里填写你自己的 API（地址 / Key / 模型）。Key 只存在本机。
4. 默认带公开「在」人设与协议；不含原作者的个人论辩记忆。
5. 首次运行会在 %APPDATA%\SimpleCode 写入设置与可编辑的 skills/rules。

请勿把本机 %APPDATA%\SimpleCode\settings.json 发给别人（内含 Key）。
